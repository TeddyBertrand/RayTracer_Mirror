#include <iostream>

int main() {
    std::cout << "Hi!" << std::endl;
    return 0;
Bah ça compile pas tout ça non ? C'est normal, c'est juste un test pour voir si le pipeline fonctionne. On va ajouter du code plus tard.